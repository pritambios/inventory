<div class="center-block form-container">
  <%= form_for @item, url: { action: "change_parent" }, html: { method: :put }, class: "form-horizontal" do |f| %>
    <div class="form-group row">
      <div class="col-sm-4"><%= f.label :change_parent, t('.change_parent'), class: 'required' %></div>
      <div class="col-sm-8"><%= f.text_field :parent_id, required: true, class: "change-parent form-control", id: "change_parent"  %></div>
    </div>
    <div class="display-item">
    </div>
    <div class="text-right">
      <button type="button" class="btn btn-default" data-dismiss="modal"><%= t('button.cancel') %></button>
      <%= f.submit t('button.update'), class: "btn btn-primary" , disabled: false %>
    </div>
  <% end %>
</div>
