<div class="page-header">
  <%= link_to 'Add', new_item_path, class: "btn btn-primary pull-right" %>
  <h3>Items(<%= @items.count %>)</h3>
</div>

<table class="table table-striped table-last">
  <tr>
    <th>Item</th>
    <th>Model Number</th>
    <th>Serial Number</th>
    <th>Status</th>
    <th>&nbsp;</th>
  </tr>
  <% @items.each do |item| %>
    <tr>
      <td><%= link_to item.name, item_path(item) %></td>
      <td><%= item.model_number %></td>
      <td><%= item.serial_number %></td>
      <td>
        <%= link_to working_status(item), status_update_path(item), remote: true, method: :put, id: "status-#{ item.id }" %>
      </td>
      <td><%= edit_link(edit_item_path(item)) %></td>
    </tr>
  <% end %>
</table>

<%= will_paginate %>
