<% if @category.errors.present? %>
  $('.modal-body .error-container').html("<%= j(render 'shared/errors', obj: @category) %>");
<% else %>
  Inventory.Modal.close();
  window.location.reload();
<% end %>
