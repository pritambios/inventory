<% if current_user %>
  <div class="card">
    <div class="card-header">
      <h3 class="card-title"><%= t('.title') %></h3>
    </div>
    <div class="card-body">
      <h3><%= t('.heading') %></h3>
    </div>
  </div>
<% end %>
