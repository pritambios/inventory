<% if @system.errors.present? %>
  $('.modal-body .error-container').html("<%= j(render 'shared/errors', obj: @system) %>");
<% else %>
  Inventory.Modal.close();
  window.location.reload();
<% end %>
