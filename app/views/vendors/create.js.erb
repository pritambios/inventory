<% if @vendor.errors.present? %>
  $('.modal-body .error-container').html("<%= j(render 'shared/errors', obj: @vendor) %>");
<% else %>
  Inventory.Modal.close();
  window.location.reload();
<% end %>
